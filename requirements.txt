# C.Y.R.U.S - Cognitive sYstem for Real-time Utility & Services
# Python 3.11+ required
# CPU-only build (no NVIDIA GPU) — install torch separately before this file:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

# ── Audio I/O ────────────────────────────────────────────────────────────────
sounddevice==0.4.6
soundfile==0.12.1
# numpy is pinned by torch — do not re-pin here to avoid conflicts

# ── Automatic Speech Recognition ─────────────────────────────────────────────
faster-whisper==1.0.3
ctranslate2==4.3.1

# ── Noise Reduction ───────────────────────────────────────────────────────────
noisereduce>=3.0.0

# ── Voice Activity Detection ──────────────────────────────────────────────────
webrtcvad-wheels>=2.0.10  # pre-built wheels for Windows (replaces webrtcvad on Win)
librosa==0.10.2

# ── Speaker Recognition ───────────────────────────────────────────────────────
speechbrain>=1.0.0
transformers>=4.40.0,<4.46.0  # >=4.46 imports torch.distributed.tensor → triggers speechbrain k2 lazy import crash
# Required by transformers 4.45.x — newer versions break the pin above
tokenizers>=0.20,<0.21
# huggingface-hub: any version >=0.23.2,<1.0 works; pip may pull 0.36.x
huggingface-hub>=0.23.2,<1.0
# Required by transformers (often not auto-installed)
requests>=2.32.0
regex>=2024.0.0
safetensors>=0.4.1

# ── Wake-word / fuzzy matching ────────────────────────────────────────────────
fuzzywuzzy==0.18.0
python-Levenshtein==0.25.1

# ── Text-to-Speech ────────────────────────────────────────────────────────────
kokoro==0.9.4
# espeak-ng must be installed via system package manager (winget install eSpeak.eSpeak-ng)
edge-tts>=7.0.0            # Microsoft Neural TTS — primary Spanish voice (es-PE-AlexNeural)
# TTS (Coqui XTTS v2) is NOT listed here — it is optionally loaded at runtime via
# xtts_tts.py:load() and will gracefully degrade if the package is absent.
# Install manually if needed: pip install TTS>=0.22.0

# ── LLM clients ───────────────────────────────────────────────────────────────
anthropic==0.28.1
httpx==0.27.0
ollama==0.2.1

# ── WebSocket server ──────────────────────────────────────────────────────────
websockets>=12.0,<14.0

# ── Microservices (TTS/ASR/Vision/Embedder servers) ──────────────────────────
fastapi==0.111.1
uvicorn[standard]==0.30.1

# ── Configuration & environment ───────────────────────────────────────────────
pyyaml==6.0.2
python-dotenv==1.0.1

# ── Async utilities ───────────────────────────────────────────────────────────
asyncio-throttle==1.0.2
aiofiles==23.2.1

# ── Utilities ─────────────────────────────────────────────────────────────────
pathlib2==2.3.7
structlog==24.1.0
rich==13.7.1
psutil>=5.9.0              # system monitoring (CPU/RAM usage in engine)
setuptools<70              # provides pkg_resources (required by ctranslate2); newer setuptools removed it
pyaudio>=0.2.14            # audio I/O backend (auto-installed on Windows via wheel)

# ── Memory (Phase 3) ──────────────────────────────────────────────────────────
qdrant-client==1.9.1
sentence-transformers==3.0.1

# ── Vision (Phase 2) ──────────────────────────────────────────────────────────
opencv-python==4.10.0.84
ultralytics==8.2.24
deepface==0.0.93
# Pillow is pinned by torch — do not re-pin here to avoid conflicts

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.8
pytest-mock==3.14.0
pytest-cov==5.0.0
